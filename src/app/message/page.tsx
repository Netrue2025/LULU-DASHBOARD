import { PublicMessagePage } from "@/components/public-message-page";

export const metadata = {
  title: "Message LULU",
  description: "Send a message to Jeremiah through LULU"
};

export default function MessagePage() {
  return <PublicMessagePage />;
}
